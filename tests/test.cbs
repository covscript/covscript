function main(start)
    define i=start
    while(i<100)
        global.println(i)
        if(i>=50)
            break
        else
            ++i
        end
    end
    return i
end
global.print("Please enter the start:")
global.println("Result:",main(input()))
