import "../stdlib/forward_list"
define list as forward_list
list.push_front("Hello")
list.push_front(0)
list.pop_front()
list.push_front("Hahaha")
list.insert_after(list.begin,1)
list.erase_after(list.begin->next)
block
	define it=list.begin
	while(it!=null)
		system.println(it->data)
		it=it->next
	end
end
